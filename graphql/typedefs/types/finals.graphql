type Finals {
    _id: ID!

    competition_id: ID!
    competition: Competition!
    category_id: ID!
    category: Category!

    participant_score1: ParticipantWithScore!
    participant_score2: ParticipantWithScore!

    round: Int!
}
