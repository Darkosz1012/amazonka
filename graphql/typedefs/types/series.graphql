type Series {
    participant_id: ID!
    participant: Participant!
    category_id: ID!
    category: Category!
    distance_id: ID!

    series_no: Int!
    was_counted: Boolean!
    score: Int!
    arrows: [String]
}
