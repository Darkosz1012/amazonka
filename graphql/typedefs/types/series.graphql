type Series {
    participant_id: ID!
    participant: Participant!
    competition_id: ID!
    competition: Competition!

    distance_id: ID!

    series_no: Int!
    was_counted: Boolean!
    score: Int!
    arrows: [String]
}
