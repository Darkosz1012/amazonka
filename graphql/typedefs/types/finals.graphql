type Finals {
    _id: ID!

    competition_id: ID!
    competition: Competition!
    category_id: ID!
    category: Category!

    participant1: ParticipantWithScore!
    participant2: ParticipantWithScore!

    round: Int!
}
